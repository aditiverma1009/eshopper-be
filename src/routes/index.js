const { healthRouter } = require("./health.route");
const { itemsRouter } = require("./items.route");
const { ordersRouter } = require("./orders.route");

module.exports = { healthRouter, itemsRouter, ordersRouter };
